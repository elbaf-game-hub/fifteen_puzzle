part of '../fifteen_puzzle.dart';

class FifteenPuzzlePage extends StatelessWidget {
  const FifteenPuzzlePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('15 Puzzle')),
      body: const Center(child: Text('Coming soon — see PLAN.md')),
    );
  }
}
